__pycache__/
*.pyc
*.pyo
.env
dist/
build/
# *.spec files are committed so do NOT ignore them globally
# (PyInstaller-generated hook files may appear in build/ which is already ignored)
.checkpoint.json
*.log
models/*.pth
models/.download_attempted
.venv/
venv/
